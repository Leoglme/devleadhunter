/**
 * A brand palette preset for the wallet card preview page — the three colors the
 * generated ``.pkpass`` uses (background / foreground / label).
 */
export type WalletCardPalette = {
  id: string
  label: string
  background: string
  foreground: string
  labelColor: string
}
