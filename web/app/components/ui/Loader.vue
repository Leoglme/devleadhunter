<template>
  <div
    class="fixed inset-0 z-50 flex items-center justify-center"
    :style="{ backgroundColor: 'var(--app-bg, #f4f1e9)' }"
  >
    <div class="loader-ring"></div>
  </div>
</template>

<script lang="ts" setup>
/**
 * Full-screen loader overlay themed by the app variables
 * (falls back to the default paper backdrop outside the dashboard shell).
 * @module components/ui/Loader
 */
</script>

<style scoped>
.loader-ring {
  width: 48px;
  height: 48px;
  border: 3px solid var(--app-line, rgba(255, 255, 255, 0.1));
  border-left-color: var(--app-accent, #e8a33c);
  border-radius: 50%;
  animation: spin 0.8s linear infinite;
}

@keyframes spin {
  to {
    transform: rotate(360deg);
  }
}
</style>
